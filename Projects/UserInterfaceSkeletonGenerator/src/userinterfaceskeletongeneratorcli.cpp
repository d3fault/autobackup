#include "userinterfaceskeletongeneratorcli.h"

//TODOreq: eventually I'll give this app a GUI. I should use this app to generate it's own GUI [interface and stub] <3. mindfuck x3 <3 I have a huge boner right now
UserInterfaceSkeletonGeneratorCli::UserInterfaceSkeletonGeneratorCli(QObject *parent)
    : QObject(parent)
{ }
