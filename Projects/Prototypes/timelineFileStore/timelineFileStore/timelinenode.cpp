#include "timelinenode.h"

TimelineNode::TimelineNode(TimelineNodeType newNodeType, QString newNodeTitle, QString newNodeContent) :
    m_Type(newNodeType), m_Title(newNodeTitle), m_Content(newNodeContent)
{

}
