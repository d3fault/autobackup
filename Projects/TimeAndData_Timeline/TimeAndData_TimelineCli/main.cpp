#include <QCoreApplication>

#error "idk if this will have a ui at all at first, this file was just created by QtCreator automatically xD. at first it will just be a lib, so if you're trying to compile this, you're doing it wrong!"

int main(int argc, char *argv[])
{
    QCoreApplication a(argc, argv);

    return a.exec();
}
