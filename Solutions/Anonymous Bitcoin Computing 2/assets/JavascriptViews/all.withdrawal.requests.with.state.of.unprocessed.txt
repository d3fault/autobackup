Map:

function(doc, meta)
{
  if(meta.id.lastIndexOf("withdrawFundsRequest_",0) === 0)
  {
    if(doc.withdrawFundsRequestState === "unprocessed")
    {
      emit(null,null);
    }
  }
}

Reduce: None